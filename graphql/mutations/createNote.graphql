mutation CreateNote(
  $title: String!
  $description: String!
  $category: String!
) {
  createNote(title: $title, description: $description, category: $category) {
    errors {
      field
      message
    }
    note {
      id
      title
      description
      category
      creatorId
    }
  }
}
